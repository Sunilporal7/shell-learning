# Functions are like variables
#variables - when we assign a name to a set of data then it is variable
#Functions -  when we assign a name to a set of commands then it is function

#Function Declare
example(){
  echo this is a example function
}
 example